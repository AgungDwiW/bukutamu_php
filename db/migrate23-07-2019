ALTER TABLE `tipe_tamu` ADD `parent` INT NULL AFTER `tipe`;
ALTER table tipe_tamu
ADD CONSTRAINT sub_tamu
FOREIGN KEY sub_tamu(parent)
REFERENCES tipe_tamu(id)
ON DELETE cascade;
ALTER TABLE `tamu` DROP `perusahaan`;
ALTER TABLE `tipe_tamu` CHANGE `parent` `parent` INT(11) NULL DEFAULT '-1';
